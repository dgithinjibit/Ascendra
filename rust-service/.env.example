# Bind locally by default; use 0.0.0.0:8091 inside a container.
SYNC_SENTA_ADAPTIVE_BIND=127.0.0.1:8091

# Set this only on the Next.js server environment after the Rust service is healthy.
# Never expose it as a browser-visible variable.
# SYNC_SENTA_RUST_ADAPTIVE_URL=http://127.0.0.1:8091
