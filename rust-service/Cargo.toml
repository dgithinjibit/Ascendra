[package]
name = "syncsenta-adaptive-service"
version = "0.1.0"
edition = "2021"

[dependencies]
syncsenta-moe-core = { path = "../rust-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
